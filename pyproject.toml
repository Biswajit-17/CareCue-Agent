[project]
name = "carecue"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "bedrock-agentcore",
    "strands-agents",
    "python-dotenv",
    "pydantic",
    "email-validator",
    "boto3",
]
